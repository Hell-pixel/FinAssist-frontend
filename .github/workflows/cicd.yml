name: CI/CD Frontend

on:
  push:
    branches:
      - 'master'
  workflow_dispatch:
    inputs:
      deploy_target:
        description: 'Select deploy step'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - registry
          - server
          - redeploy

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    name: Build and Test

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Clean install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Build the application
        run: npm run build

      - name: Run tests (placeholder)
        run: echo "No tests available yet"

  push-to-registry:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.deploy_target == 'all' || github.event.inputs.deploy_target == 'registry') }}
    needs: build-and-test
    runs-on: ubuntu-latest
    name: Push Docker image to registry

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ vars.DEV_VERSION }}

  deploy-to-server:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.deploy_target == 'all' || github.event.inputs.deploy_target == 'server') }}
    needs: push-to-registry
    runs-on: ubuntu-latest
    name: Deploy image on server

    steps:
      - name: SSH and deploy container
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            IMAGE=${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ vars.DEV_VERSION }}
            echo "Pulling image: $IMAGE"
            docker pull $IMAGE
            docker stop finass_frontend || true
            docker rm finass_frontend || true
            docker run -d \
              --name finass_frontend \
              --network "proxy" \
              --restart unless-stopped \
              $IMAGE

  force-redeploy:
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.deploy_target == 'redeploy' }}
    runs-on: ubuntu-latest
    name: Force redeploy to server (no build)

    steps:
      - name: SSH and redeploy container
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            IMAGE=${{ vars.REGISTRY }}/${{ vars.IMAGE_NAME }}:${{ vars.DEV_VERSION }}
            echo "Re-deploying image: $IMAGE"
            docker pull $IMAGE
            docker stop finass_frontend || true
            docker rm finass_frontend || true
            docker run -d \
              --name finass_frontend \
              --network "proxy" \
              --restart unless-stopped \
              $IMAGE
